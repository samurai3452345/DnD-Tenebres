spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rpg_db
    username: postgres
    password: 1
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml